@startuml si1.3-update-user-password
title SI1.3 Update user password

start
:Receive update password command;

if (request and password valid?) then ([Yes])
  :Load user account;
  :Hash password;
  :Store password credential;
  :Return success;
  stop
else ([No])
  :Return failure;
  stop
endif

@enduml
