business/
    business-processes/
        *.md (one markdown file per business process)
        diagrams/
            *.bpmn (all BPMN diagram files go here directly, no subfolders)
            *.png (BPMN image exports)
    system-interactions/
        *.md (one markdown file per system interaction)
        diagrams/
            *.pu (all PlantUML diagram files go here directly, no subfolders)
            *.svg, *.png (PlantUML image exports)
